$(document).ready(function() {
    // Sayfa yüklendiğinde yapılacak işlemler buraya yazılabilir

    // Örnek: Bir düğmeye tıklandığında bir alert kutusu göster
    $('#myButton').click(function() {
        alert('Button clicked!');
    });
});
